class Solution:
    def longestCommonPrefix(self, strs: List[str]) -> str:
       shortest_str = min(strs, key=len)
       for i in range(len(shortest_str)):
            for _str in strs:
                if _str[i] != shortest_str[i]:
                    return shortest_str[:i]
       return shortest_str
