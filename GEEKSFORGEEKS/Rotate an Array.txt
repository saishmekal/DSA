class Solution:
	def rotateArr(self, arr, d):
		n = len(arr)

		d %= n

		temp = [0] * n

		for i in range(n - d):
			temp［i］ = arrd +i］
		for i in ranged):
			temp[n - d + i] = arr[i]
		for 1 in rangen):
			arr [i] = temp[i]