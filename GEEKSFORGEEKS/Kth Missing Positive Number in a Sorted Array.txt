class Solution:
    def kthMissing(self, arr, k):
        
        n = len(arr)
        for i in range(n):
            if arr[i] > (k + i):
                return k + i
          
    
        return k + n


        